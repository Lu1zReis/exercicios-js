const nome = 'João';

/*
    com a const, não podemos reatribuir ela outro valor.
    por exemplo: nome = 'Luiz';
*/

const val1 = 2;
const val2 = 3;
const resu = val1 * val2;

console.log(resu);

// verificando os tipos de dados (typeof)
console.log(typeof(nome));
console.log(typeof(resu));
